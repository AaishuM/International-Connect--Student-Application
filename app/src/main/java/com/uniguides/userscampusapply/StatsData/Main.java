package com.uniguides.userscampusapply.StatsData;

public class Main {
    public static void main(String[] args) {
        //SignUp activity = new Sig();
       // Context context = activity.getContext();
       // FrameLayout layout = activity.getLayout();

        // Now you can use the context and layout in your code
      //  int[] data = StatisticsHelper.getRandomData();
       // StatisticsGraph.drawBarChart(data, "International Students by Country", "Country", "Number of Students", context, layout);
    }
}
